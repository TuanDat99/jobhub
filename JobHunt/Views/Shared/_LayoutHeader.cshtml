
<header class="stick-top forsticky">
    <div class="menu-sec">
        <div class="container">
            <div class="logo">
                <a href="/" title=""><img class="hidesticky" src="~/Assets/client/images/resource/alo.png" alt="" /><img class="showsticky" src="~/Assets/client/images/resource/alo.png" alt="" /></a>
            </div><!-- Logo -->
            @Html.Partial("_LayoutAuthentication")
            <nav>
                <ul>
                    <li class="menu-item-has-children">
                        <a href="@Url.Action("ListJobs","RecruitJob")" title="">All Jobs</a>
                    </li>
                    @if (Request.IsAuthenticated)
                    {
                        if (!User.IsInRole("Người tìm việc"))
                        {
                            <li class="menu-item-has-children">
                              
                            </li>
                        }
                        else
                        {
                            <li class="menu-item-has-children">
                                <a href="@Url.Action("ListRecruits", "Recruit")" title="Danh sách công ty">Company</a>
                            </li>
                        }
                    }
                    else
                    {
                        <li class="menu-item-has-children">
                            <a href="@Url.Action("ListRecruits","Recruit")" title="Danh sách công ty">Company</a>
                        </li>
                    }
                    
                </ul>
            </nav><!-- Menus -->
        </div>
    </div>
</header>